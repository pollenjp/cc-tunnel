# PlantUML 直近変更色分け再評価 v2

## 概要

`cmd_cctunnel_puml_highlight_recent_changes_v2` に基づき、`docs/plantuml/` の
全 .puml ファイルに対して最新 git 履歴基準で直近変更の色分け強調を実施した。

## 直近の定義（基準コミット）

| 項目 | 値 |
|------|-----|
| 基準コミット | `95a54d5` |
| コミット日時 | 2026-04-30 04:24 (JST) |
| コミット概要 | `docs: 実装変更に追従して docs・PlantUML 図を一括同期` |
| 親コミット | `32e395e` |

「直近」= コミット `32e395e` と `95a54d5` の差分（1コミット分）。

## 前回強調との差分

前回 cmd（cmd_cctunnel_puml_highlight_changes、04:35 実施）は未コミットのまま終了しており、
現在のファイルにハイライトは存在しなかった。今回は同一基準コミット `95a54d5` で再評価・実施。

## 各ファイルの更新内容

### 変更ファイル（5件）

#### 1. `auth_flow.puml` — 変更 (`#FFD700`)

- `#FFD700:SSE /auth/pty/stream?conversationId=... Subscribe;`
- `#FFD700:text/event-stream → xterm.js renderer に表示;`
- `#FFD700:SSE ストリーム終了;`

変更内容: polling（GET /auth/output）から SSE ストリーム（/auth/pty/stream）への移行を反映。

#### 2. `credential_session_start_sequence.puml` — 変更 (`#FFD700`)

ループ内の polling → SSE 変更箇所を `group #FFD700 変更: ポーリング → SSE ストリーム` でラップ:
- `fe -> handler: GET /auth/pty/stream?conversationId=... (SSE)`
- `handler -> container: remoteClient.SubscribePTYStream`
- `container --> handler: PTY stdout (SSE events)`
- `handler --> fe: SSE event stream`
- `fe -> user: ターミナル UI 出力 (xterm.js renderer)`

#### 3. `docker_gce_component.puml` — 変更 (`#FFD700`)

役割説明コメント（4行）が追加されたが PlantUML 描画には影響しない。
浮動ノートで変更を明示。

#### 4. `docker_gce_multi_container.puml` — 変更 (`#FFD700`)

同上。役割説明コメント追加。先頭ノートで変更を明示。

#### 5. `usecase.puml` — 変更 (`#FFD700`)

UC_AuthOutput のエンドポイントが `GET /auth/output (polling)` → `GET /auth/pty/stream (SSE)` に変更。
`<<changed>>` ステレオタイプ + `skinparam usecase<<changed>>` で #FFD700 強調を追加。凡例ブロックも追加。

### 新規ファイル（2件）

#### 5. `local_provider_session_start.puml` — 新規追加 (`#90EE90`)

EXECUTION_PROVIDER=local のセッション起動フローを示す新規シーケンス図。
先頭にヘッダーノート（`#90EE90`）を追加して新規ダイアグラムであることを明示。

#### 6. `local_vs_docker_gce_comparison.puml` — 新規追加 (`#90EE90`)

local vs docker_gce モード差異比較図（deployment/component 混合図）。
浮動ノート（`#90EE90`）で新規ダイアグラムであることを明示。

### 変更なし（13件）

以下のファイルはコミット `95a54d5` で変更なし。ハイライトなし、凡例なし。

- `c4_component.puml`
- `c4_container.puml`
- `c4_context.puml`
- `chat-activity.puml`
- `docker_gce_architecture.puml`
- `docker_gce_idle_cleanup.puml`
- `docker_gce_session_start.puml`
- `sandbox_architecture.puml`
- `sandbox_idle_cleanup.puml`
- `sandbox_selection_flow.puml`
- `sandbox_session_start.puml`
- `screen_navigation.puml`
- `useConversationPoller-activity.puml`

## 凡例設計

全変更ファイルに以下の凡例ブロックを追加:

```plantuml
legend right
  **凡例 (基準コミット: 95a54d5)**
  |<#FFD700>  | 変更された要素 |
  |<#90EE90>  | 新規追加の要素 |
endlegend
```

## plantuml 構文チェック結果

| 対象 | 結果 |
|------|------|
| 変更・新規 7 ファイル | PASS |
| 使用コマンド | `mise run plantuml:run -- -checkonly <files>` |
| エラー・警告 | なし |
| 改行コード | 全ファイル LF のみ (CRLF なし) |
