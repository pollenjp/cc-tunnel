name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test-cc-tunnel:
    name: "cc-tunnel: test"
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/cc-tunnel
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: apps/cc-tunnel/go.mod

      - name: Test
        run: go test ./...

  lint-cc-tunnel:
    name: "cc-tunnel: lint"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: apps/cc-tunnel/go.mod

      - name: Lint
        uses: golangci/golangci-lint-action@v7
        with:
          version: v2.11.4
          working-directory: apps/cc-tunnel

  test-cc-tmux-tunnel:
    name: "cc-tmux-tunnel: test"
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/cc-tmux-tunnel
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: apps/cc-tmux-tunnel/go.mod

      - name: Test
        run: go test ./...

  lint-cc-tmux-tunnel:
    name: "cc-tmux-tunnel: lint"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: apps/cc-tmux-tunnel/go.mod

      - name: Lint
        uses: golangci/golangci-lint-action@v7
        with:
          version: v2.11.4
          working-directory: apps/cc-tmux-tunnel

  test-frontend:
    name: "frontend: test"
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/frontend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: apps/frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Test
        run: npm run test

  lint-frontend:
    name: "frontend: lint"
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/frontend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: apps/frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npx tsc --noEmit