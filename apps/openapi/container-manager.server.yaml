# oapi-codegen config for container-manager (HTTP server).
# Used by apps/container-manager/internal/api/handler.go via go:generate.
package: api
generate:
  std-http-server: true
  models: true
  strict-server: true
  embedded-spec: true
output-options:
  skip-prune: false
